import React  from "react";

const Header = () => {
    return(
        <div className = "App-header">
            <h1 id = "header-text">CV builder with React</h1>
        </div>
    );
};
export default Header;